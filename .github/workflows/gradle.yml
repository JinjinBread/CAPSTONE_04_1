## This workflow uses actions that are not certified by GitHub.
## They are provided by a third-party and are governed by
## separate terms of service, privacy policy, and support
## documentation.
## This workflow will build a Java project with Gradle and cache/restore any dependencies to improve the workflow execution time
## For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-gradle
#
#name: Java CI with Gradle
#
#on:
#  push:
#    branches: [ "main" ]
#  pull_request:
#    branches: [ "main" ]
#
##워크 플로우가 깃 레포에 대한 권한을 읽기 만 가능하게 설정한다.
#permissions:
#  contents: read
#
#jobs:
#
#  build:
#    runs-on: ubuntu-latest
#
#    steps:
#      - uses: actions/checkout@v4
#      - name: Set up JDK 17
#        uses: actions/setup-java@v4
#        with:
#          java-version: '17'
#          distribution: 'temurin'
#
#      - uses: actions/checkout@v4
#      - run: touch ./src/main/resources/application.properties
#      - run: echo "${{ secrets.APPLICATION_PROPERTIES }}" > ./src/main/resources/application.properties
#      - run: cat ./src/main/resources/application.properties
#        # Configure Gradle for optimal use in GiHub Actions, including caching of downloaded dependencies.
#        # See: https://github.com/gradle/actions/blob/main/setup-gradle/README.md
#        # - name: Setup Gradle
#        #   uses: gradle/actions/setup-gradle@v3
#
#      - name: Setup MySQL
#        uses: mirromutth/mysql-action@v1.1
#        with:
#          character set server: 'utf8'
#          collation server: 'utf8_general_ci'
#          mysql database: 'capstone_04_1' # Optional, default value is "test". The specified database which will be create
#          mysql root password: ${{ secrets.MYSQL_ROOT_PASSWORD }} # Required if "mysql user" is empty, default is empty. The root superuser password
#          mysql user: 'capstone' # Required if "mysql root password" is empty, default is empty. The superuser for the specified database. Can use secrets, too
#          mysql password: ${{ secrets.MYSQL_USER_PASSWORD }} # Required if "mysql user" exists. The password for the "mysql user"
#
#      - name: Grant execute permission for gradlew
#        run: chmod +x ./gradlew
#
#      - name: Build with Gradle
#        uses: gradle/gradle-build-action@v3
#        with:
#          arguments: build
#
#      - name: Docker build
#        run: |
#          docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
#          docker build -t spring-cicd .
#          docker tag spring-cicd ${{ secrets.DOCKER_USERNAME }}/spring-cicd:${GITHUB_SHA::7}
#          docker push ${{ secrets.DOCKER_USERNAME }}/spring-cicd:${GITHUB_SHA::7}
#
#      - name: Deploy
#        uses: appleboy/ssh-action@v1.0.3
#        with:
#          host: ${{ secrets.EC2_HOST }} # EC2 인스턴스 퍼블릭 DNS
#          username: ${{ secrets.EC2_USERNAME }}
#          key: ${{ secrets.EC2_SSH_KEY }} # pem 키
#          port: ${{ secrets.EC2_SSH_PORT }} # 접속포트번호 (보통 22)
#          envs: GITHUB_SHA
#          script:
#            cd /home/ubuntu/CAPSTONE_04_1
#            git pull